// TODO this function should be served from our website
function addVaiPay({ target = '', options = {}}) {
  const targetElem = document.querySelector(target);

  if (targetElem) {

  } else {
    console.warning('VAI Pay : Could not find DOM element', target)
  }

}
